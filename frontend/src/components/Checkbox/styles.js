import styled from 'styled-components';

export const StyledCheckbox = styled.div`
  display: flex;
  align-items: baseline;
  margin-bottom: 10px;

  input {
    transform: scale(1.3);
    bottom: 3px;
  }
`;
